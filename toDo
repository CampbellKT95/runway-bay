Token is now successfully saved, but I have to refresh to see all tenants once I have been pushed to /tenants
    I think video covers this issue.


###################

Place Google Auth code in an extra folder & add to .gitignore

Add button to upload lease document for each tenant
(should also probably add a button on <SingleTenant /> to access it)

## FINALE

Re-deply backend to heroku and frontend to netlify
